# TODO: main commands for docker and start
docker-compose up --build --no-recreate -d
docker-compose up -d
docker exec -it medlab_docker sh

# TODO: h1: 40px, h2: 36px, h3: 28px; span: 24px?? -> 18px;
# TODO: make button, textbox, gradient!!!